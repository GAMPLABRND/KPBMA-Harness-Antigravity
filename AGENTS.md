# URS → GMP MVP 원샷 빌드 하네스 (60분, Codex 전용)

## 0. 읽는 법 (Codex CLI)

- 이 폴더는 **Codex 전용 하네스**다. Codex는 이 AGENTS.md를 자동으로 읽는다. Claude Code와 Antigravity CLI 조는 `05 Harness`를 쓴다.
- 서브에이전트 정의는 `agents/`의 7개 파일(analyzer-a, b, c, builder-d1, d2, d3, d4)이다. 서브에이전트는 이 파일을 스스로 읽지 않으므로,
  **서브에이전트를 띄울 때 해당 파일의 전문을 프롬프트에 그대로 넣고**, 첫 줄에 `담당 소절: 7.1.x, 7.1.y`(분석)나 `PLAN.md와 SPEC 경로`(구현)를 적고,
  2절 언어 규칙과 8절 기술 규칙, 파일 소유권, `design.md` 경로를 함께 넣는다. 분석 3개 동시 → 구현 4개 동시.
- **서브에이전트는 완료 보고를 낼 때까지 기다린다.** STEP 예산은 목표이지 서브에이전트를 중단하거나 결과를 일찍 수거하는 기준이 아니다.
  서브에이전트를 쓸 수 없거나 실패하면 같은 파일을 순서대로 직접 수행한다(analyzer-a → b → c → builder-d1 → d2 → d3 → d4). 이때 STEP 3 예산은 2배(66분)까지 허용한다.
- **턴을 끊지 않는다.** 원샷 빌드는 STEP 4의 완료 보고까지 한 턴에 끝낸다. "계속할까요?", "다음 단계로 진행할까요?" 같은 확인으로 턴을 끝내지 않는다.
  진행 상황은 STEP마다 영어 한 줄로만 알린다. 사용자가 "계속"이라고 하면 멈춘 지점부터 이어서 한다.
- 파일 편집: apply_patch의 컨텍스트가 맞지 않으면 같은 패치를 반복하지 말고 파일 전체를 다시 쓴다. 줄바꿈은 LF(`.gitattributes`), 인코딩은 UTF-8.
- 명령 실행: `node scripts/convert-urs.mjs`, `npm run build`, `npm run dev`(백그라운드 유지)를 쓴다. 전체 자동 모드로 실행되므로 승인을 기다리지 않는다.
- 화면 디자인은 `design.md`를 따른다. 구현 완료 후 FDS 작성(Word 출력)은 `docs/FDS_GUIDE.md`, DQ, IOQ, RTM 초안은 `docs/DRAFTS.md`를 따른다.
- 푸시와 배포는 AI가 하지 않는다. `git push`를 실행하지 않는다. 커밋과 태그까지만 만들고 사람이 터미널에서 푸시한다(10절).

| 모드 | 언제 | 절 |
|---|---|---|
| ① 원샷 빌드 | URS를 넣고 처음 만들 때 (60분, 한 턴) | 3 ~ 8 |
| ② 수정 요청 | 개발 서버에서 QA하며 오류, 화면, 문구를 고칠 때 (반복) | 9 |
| ③ 커밋 준비 | 사용자가 명시적으로 요청할 때 (푸시와 배포는 사람이) | 10 |
| ④ FDS 작성 | QA와 수정이 끝나 구현이 완료되었을 때. 현재 상태를 FDS로 쓰고 Word 문서로 낸다 | 13, `docs/FDS_GUIDE.md` |
| ⑤ 문서 초안 | DQ, IOQ, RTM 초안을 요청할 때 | `docs/DRAFTS.md` |

## 1. 미션

`docs/urs/`의 URS(KPBMA-EDU-001-URS 양식)를 읽고 **60분 이내에** 동작하는 GMP MVP 웹앱을 한 번에 일괄 생성한다.
스택: Next.js(App Router) + TypeScript + Tailwind. DB: Google Sheets. 배포: Vercel. 검증(IOQ)은 교육생이 수행하므로
생성 단계에서 검증 루프를 돌지 않는다. URS 7.1의 모든 조항이 구현 대상이며, 구현하지 못한 조항은 사유와 함께 보고한다(12절).

이 하네스 하나로 조별로 배정된 3종의 시스템을 만든다. 시스템의 종류, 엔티티, 탭, 상태값, 역할, 계정은 전부 URS가 정한다.

| 시스템 | 검증의 축 | URS가 요구하는 차단 규칙의 예 |
|---|---|---|
| 장비사용기록서 (전자로그북) | 기록 통제 | 교정 만료 장비 사용, 중복 사용, 승인 후 수정, 시간 역전 |
| 세척밸리데이션 관리 | 계산 검증 | 경계값, 규격 외 입력, 판정 결과의 임의 변경 |
| 실험실 재고관리 (시약, 표준품) | 상태 통제 | 음수 재고, 기한 경과품 출고, 재고와 유효기간의 정합 |

## 2. 언어, 문체, 표기 규칙

1. **진행 중 출력은 영어**: STEP 진행 메시지, 중간 판단, 서브에이전트 보고, 작업 파일(SPEC_A/B/C.md)은 간결한 영어로 쓴다.
   URS에서 인용하는 한국어 문구(화면 안내 문구, 라벨, 상태명)는 원문 그대로 한국어를 유지한다.
2. **최종 보고와 산출물은 한국어 보고용 문체**: 완료 보고, 문제와 미구현 사유의 설명, 산출물(PLAN.md, IMPLEMENTED.md, DECISIONS.md,
   CHANGELOG.md, README.md, docs/*_DRAFT.md)은 한국어로 쓰며 "~하였습니다", "~입니다", "~되어 있습니다"의 보고체를 쓴다.
   "~한다", "~해라" 같은 평서형과 명령형을 산출물과 보고에 쓰지 않는다. 화면 문구는 한국어(design.md 9절).
   예외: FDS 본문(`docs/FDS.md`)은 규격서 평서형("~한다", "~된다")으로 쓴다(13절).
3. 방점(·)을 쓰지 않고 콤마로 나열한다. 대시(—, –)를 쓰지 않고 콤마, 괄호, 콜론, "및", "또는"으로 풀어 쓴다.
   하이픈(-)은 목록 기호, 파일명, 코드, 영문 복합어에만 쓴다. 이모지와 장식 기호를 쓰지 않는다. 화살표(→)는 상태 흐름 표기에만 쓴다.
4. 용어는 데이터 완전성(무결성 금지), 규격서(명세서 금지), 감사추적, 전자서명, 기준정보, 조항 ID. URS의 용어를 그대로 쓴다.
5. 산출물에는 URS 조항 ID를 반드시 병기한다. 근거 조항이 없는 항목은 "URS 근거 없음, 기본값"으로 적는다.
6. 커밋 메시지는 한국어로 "무엇을 왜 (URS-F-nnn)" 형식이다.

## 3. 절대 규칙 (원샷 빌드 STEP 0~4, 위반 금지)

1. 질문 금지, 확인 대기 금지. 모호하면 합리적 기본값을 선택하고 `DECISIONS.md`에 기록 후 진행한다.
2. 테스트 코드 작성 금지, 단계별 검증 루프 금지.
3. `npm run build`는 최대 2회. 2회차에도 실패하는 기능은 축소하거나 주석 처리하고 기동을 우선한다. 축소는 미구현 보고에 적는다.
   빌드 오류 수정 중 apply_patch가 실패하면 해당 파일을 전체 재작성한다.
4. 리팩토링, 최적화, 패키지 추가 금지(googleapis 외 추가 설치 금지, UI는 Tailwind와 `components/ui.tsx`만).
5. 서브에이전트 병렬 실행이 가능하면 반드시 병렬(분석 3개 동시 → 구현 4개 동시). 서브에이전트 프롬프트에는 `agents/` 정의 파일 전문을 넣는다. 병렬 기능이 없으면 같은 분할을 순차 처리한다.
6. 파일 소유권: 각 구현 에이전트는 배정 파일만 생성, 수정한다. 공유 파일(`types.ts`, `lib/*`, `app/layout.tsx`, `components/*`,
   `app/globals.css`, `package.json`)은 오케스트레이터만 수정한다. `lib/sheets.ts`는 누구도 수정하지 않는다.
7. 각 STEP 예산 초과가 예상되면 범위를 줄여서라도 다음 STEP으로 넘어간다.

수정 요청 모드(9절) 이후에는 사용자와 대화하며 진행하므로 규칙 1은 적용하지 않는다. 규칙 4와 `lib/sheets.ts` 수정 금지는 항상 적용한다.

## 4. URS 읽는 법 (KPBMA-EDU-001-URS 양식)

- 교육생이 `docs/urs/`에 URS `.docx`를 넣는다. `node scripts/convert-urs.mjs`가 `docs/urs/*.md`로 변환한다. 이미 `.md`가 있으면 그것을 쓴다.
- 구현 기준 절은 **Section 2, Section 6, Section 7.1**이다. 7.2 ~ 7.8은 참고한다. 1, 3, 4, 5, 8, 9는 문서 형식 절이므로 읽지 않는다.

| 절 | 내용 | 쓰임 |
|---|---|---|
| 2 범위 | 2.2 사용 목적(시스템을 한 문단으로 설명) 등 | 시스템의 정체, 시스템명과 조 번호(`lib/brand.ts`의 SYSTEM_NAME, TEAM_NO), 업무 흐름 |
| 6 사용자 역할 및 권한 | 6.1 역할 정의(역할 코드와 한글 명칭), 6.2 메뉴 및 화면 구성, 6.3 권한 매트릭스(화면, 기능, 역할별 O/△/X) | 역할 코드, 계정, 메뉴, 접근 통제 |
| 7.1 기능 요구사항 | 7.1.1 ~ 7.1.N 소절, 표의 첫 열이 조항 ID(`URS-F-001` 형식) | 구현 대상 조항 전부 |
| 7.2 ~ 7.8 | 데이터, 기술, 인터페이스, 비기능, 환경(시드 계정과 시드 데이터 `URS-E-nnn`), 제약, 라이프사이클 | 시드, 표기 형식 |

- 조항 ID는 표의 첫 열이다. 산출물은 이 ID로 추적한다. ID 열이 비어 있으면 7.1 소절 순서대로 `URS-F-001`부터 임시로 매기고 `DECISIONS.md`에 기록한다.
- **역할과 계정은 URS가 정한다.** 역할 코드와 한글 명칭은 §6.1, 계정 ID와 이름은 §7.6(또는 §6)을 그대로 쓴다. URS에 계정이 없으면
  역할마다 한 계정씩 만들고(ID는 역할 코드 소문자) `DECISIONS.md`에 적는다. 템플릿의 ADMIN/USER/REVIEWER와 admin/user/reviewer는 자리표시 기본값일 뿐이다.
  **첫 빌드에서 모든 계정의 비밀번호는 `1234`로 통일한다**(`lib/brand.ts`의 INITIAL_PASSWORD). 배포 전에 사용자가 비밀번호 변경 화면에서 바꾼다.
- 7.1 소절 배정(STEP 0에서 오케스트레이터가 정한다): 소절 제목의 주제로 나눈다. analyzer-a는 기준정보, 마스터, 장비, 품목, 계정, 사용자, 보안, 접근
  관련 소절과 §2, §6 전체. analyzer-b는 기록, 등록, 입출고, 재고, 계산, 판정, 워크플로우, 검토, 승인, 전자서명, 조회, 출력, 인쇄, 대시보드, 알람 관련 소절과
  §6.2(애매한 소절은 b). analyzer-c는 데이터 완전성, 감사추적, 보존, 백업 관련 소절과 §7.2 ~ 7.8. 호출 프롬프트 첫 줄에 `담당 소절: 7.1.x, 7.1.y`를 넣는다.

## 5. 디자인 적용

- 모든 화면은 `design.md`를 따른다. 색은 `app/globals.css`의 토큰 클래스만(`bg-primary`, `text-ink-muted`, `border-line`, `rounded-card` 등), 부품은 `components/ui.tsx`만 조합한다.
- 화면 골격(상단바, 사이드 메뉴, 풋터)은 `app/layout.tsx`에 있다. STEP 2에서 `MENUS`를 URS §6.2, §6.3대로 채운다.
- **시스템 제목은 "CSV실습과정 [조번호]조 [시스템명]" 형식으로 고정**한다(예: "CSV실습과정 1조 전자로그북"). `lib/brand.ts`의 `TEAM_NO`(URS 표지 또는 §1.1의 "N조". 없으면
  조별 폴더명 team0N의 N, 그것도 없으면 1로 두고 DECISIONS.md에 기록)와 `SYSTEM_NAME`(URS 시스템명을 짧게: 전자로그북, 세척밸리데이션 관리, 실험실 재고관리 등)만 바꾸고,
  `APP_NAME`의 형식과 브라우저 탭, 로그인 화면, 상단바, 대시보드 제목에 쓰이는 자리는 바꾸지 않는다. "한국제약바이오협회 1조 전자로그북"처럼 URS 표지 문구를 그대로 제목에 쓰지 않는다.
- CI(`public/kpbma-ci.png`, `kpbma-ci-white.png`)와 교육용 목업 고지는 그대로 둔다. 인쇄 화면 머리글에 파란 CI를 넣는다(design.md 8절).
- **표가 든 카드는 좌우로 나란히 두지 않는다**(전체 폭 1열, 위아래 배치. 좌우 grid는 KPI 타일과 표 없는 요약 카드만, grid 셀에 min-w-0).
  표는 `Table`의 `columns`로 열 폭을 고정하고 셀은 `Td`(nowrap, code, num, clamp)로 정렬하며, 넘치는 내용은 행 끝 "상세"의 `Modal`과 `DescList`로 보여 준다(design.md 6.2, 6.5, 6.6).
- 로그인 화면은 템플릿(`app/login`)의 구조를 그대로 유지한다: **계정 선택 드롭다운 → 아이디 → 비밀번호 → 로그인** 순서이며, 드롭다운에서 계정을 고르면 아이디 칸이 채워지고
  비밀번호가 초기값 1234인 계정이면 비밀번호 칸도 함께 채워진다(바꾼 계정은 직접 입력). 드롭다운만 두고 아이디 칸을 없애거나, 비밀번호만 채우는 형태로 바꾸지 않는다. 빌더는 안내 문구만 URS에 맞게 고친다.

## 6. 시간 예산 (총 60분)

| STEP | 내용 | 예산 |
|---|---|---|
| 0 | URS 확인, 변환, 소절 배정, 환경 확인 | 3분 |
| 1 | URS 병렬 분석 (analyzer-a/b/c 동시) | 6분 |
| 2 | 설계 고정: 시트 스키마, 역할과 계정, 메뉴, 파일 소유권, 공통 파일 | 6분 |
| 3 | 병렬 구현 (builder-d1~d4 동시) | 33분 |
| 4 | 통합 → 빌드(2회 이내) → 기동 → 스모크 1회 → 산출물과 보고 | 12분 |

## 7. 원샷 빌드 STEP 0 ~ 4

트리거 문장: **"하네스 절차대로 URS MVP 빌드를 시작해."**

### STEP 0. 준비 (3분)
1. `docs/urs/*.md`가 있으면 그것을 쓴다. `.docx`만 있으면 `node scripts/convert-urs.mjs`를 실행하고 "조항 ID N개 탐지"를 확인한다.
2. 4절대로 7.1 소절을 a/b/c에 배정하고, §6.1 역할 코드와 §7.6 계정, §2의 시스템명과 표지의 조 번호를 확인한다.
3. `.env.local`의 `GOOGLE_SERVICE_ACCOUNT_EMAIL`, `GOOGLE_PRIVATE_KEY`, `GOOGLE_SHEET_ID`를 확인한다. 없어도 멈추지 않고 STEP 4의 스모크만 건너뛴다.

### STEP 1. URS 병렬 분석 (6분)
`agents/analyzer-a.md`, `analyzer-b.md`, `analyzer-c.md` 전문을 각 서브에이전트 프롬프트에 넣고 동시에 실행한다(첫 줄에 담당 소절). 각 에이전트는 담당 절만 읽고 구현 스펙을 영어로 쓴다(필드명 snake_case 키와 한글 라벨, 상태값 코드와 한글, 차단 규칙과 URS 원문 안내 문구, 계산식과 판정 기준).
analyzer-a → `SPEC_A.md`(역할, 계정, 화면과 역할 매트릭스, 기준정보 필드, 접근 통제), analyzer-b → `SPEC_B.md`(화면 목록, 업무 엔티티 필드 전부, 상태 머신,
계산과 판정, 차단 규칙, 전자서명과 출력, 대시보드와 알람), analyzer-c → `SPEC_C.md`(감사추적 이벤트와 필드, 데이터 완전성 규칙, 시드 데이터, KST, 비기능).

### STEP 2. 설계 고정 (6분, 오케스트레이터 단독)
`PLAN.md`(한국어 보고체)를 쓰고 공통 파일을 정비한다. 이후 구현 에이전트는 PLAN.md와 SPEC만 참조한다(URS 재독 금지).
1. 시트 탭 스키마 확정: 탭명 영문 대문자, 1행 헤더 snake_case, 첫 컬럼 `id`. 공통 탭 `USERS`(id, user_id, name, password, role, status, created_at),
   `AUDIT`(id, category, actor_id, actor_name, role, action, target, before_value, after_value, reason, timestamp_kst). 업무 탭은 SPEC_B대로. 행 삭제 없음(Append-only).
2. `types.ts`의 ROLES와 ROLE_LABELS를 URS §6.1로 교체, 상태와 엔티티 타입 추가. `lib/brand.ts`의 TEAM_NO와 SYSTEM_NAME을 URS대로(APP_NAME 형식 "CSV실습과정 N조 시스템명" 유지). `app/layout.tsx`의 MENUS를 §6.2, §6.3대로.
3. 파일 소유권 표(builder-d1~d4별 경로, 겹침 없음)와 계정표(URS 계정, 비밀번호 1234)를 PLAN.md에 적는다.
4. `lib/sheets.ts`는 그대로 쓴다(getRows, appendRow, updateRowById, ensureTab 네 함수만으로 데이터 접근). 함수 사용 규칙과 원문은 `docs/SHEETS_SPEC.md`에 있다.
   파일이 바뀌었거나 없으면 `npm run check:sheets -- --restore`로 복원한다.

### STEP 3. 병렬 구현 (33분, 순차 처리 시 66분까지)
`agents/builder-d1.md` ~ `builder-d4.md` 전문을 각 서브에이전트 프롬프트에 넣고 동시에 실행한다. 네 에이전트가 모두 완료 보고를 낼 때까지 기다린다.
- builder-d1(인증, 계정, 기준정보): 로그인 보강, 계정 관리(등록, 역할, 활성과 비활성, 잠금 해제, 비밀번호 초기화), 비밀번호 변경, 기준정보 CRUD(삭제는 비활성), 역할별 접근 차단.
- builder-d2(핵심 업무 기록): 등록, 목록, 상세, 자동 계산과 판정, 필수값 검증, 수정과 무효 처리, 서버 라우트의 차단 규칙.
- builder-d3(워크플로우, 전자서명, 출력): 상태 전이 API(허용 외 거부, 승인 후 잠금), 요청, 승인, 반려, 수정 요청, 전자서명(비밀번호 재입력), 인쇄 화면, 문서번호.
- builder-d4(감사추적, 시드, 대시보드, 문서): 감사추적 조회와 보고서 출력, `/api/seed`(전체 탭, URS 계정, 비밀번호 1234, URS §7.6 시드 데이터, 멱등), 대시보드, 알람 화면, README.
모든 변경성 API는 `lib/audit.ts`로 AUDIT 기록을 남긴다. 완료 시 각자 생성 파일과 구현한 조항 ID, 구현하지 못한 조항 ID와 사유를 영어로 보고한다.

### STEP 4. 통합, 빌드, 기동, 산출물 (12분)
1. `npm run check:sheets`로 `lib/sheets.ts`가 변경되지 않았음을 확인한다(MISMATCH면 복원). import 정합 점검 → `npm run build`(최대 2회) → `npm run dev`.
2. 스모크 1회(env가 있을 때만): `/api/seed` → 계정 선택 로그인(비밀번호 1234) → 기록 1건 저장 → 시트 행 확인. 실패 시 원인 1개만 수정 후 재확인 1회.
   화면 점검: 표가 든 카드가 좌우로 나란히 있지 않은지, 스크롤바가 보이는 표가 없는지 확인한다.
3. 산출물(한국어 보고체): `IMPLEMENTED.md`, `DECISIONS.md`, `PLAN.md`, `CHANGELOG.md`(첫 행은 원샷 빌드 완료). 형식은 11절.
4. 완료 보고(12절). 마지막에 "다음 단계: 개발 서버(localhost:3000)에서 QA 후 수정 요청 모드로 진행"을 안내한다.

## 8. 기술 규칙 (전 모드 공통)
- 모든 라우트 핸들러와 데이터 페이지 상단: `export const dynamic = "force-dynamic"; export const revalidate = 0; export const runtime = "nodejs";`
- 클라이언트에서 Sheets 직접 호출 금지. 반드시 `/api/*` 경유, fetch는 `cache: "no-store"`. 폴링(setInterval) 금지, 목록에는 수동 새로고침 버튼.
- 시각: 저장은 ISO, 표시는 `lib/kst.ts`(`YYYY-MM-DD HH:mm (KST)`, URS가 초를 요구하면 초 포함). 세션: httpOnly 쿠키 `userId|role`, 서버 컴포넌트에서 확인, 미로그인 시 `/login`.
- localStorage, sessionStorage 저장 금지. UI 한국어, URS 안내 문구는 원문 그대로.
- 클라이언트 컴포넌트의 렌더 경로에서 `Date.now()`, `new Date()`, `Math.random()`, `toLocaleString` 호출 금지(서버에서 계산해 props로 전달, 불가피하면 `useEffect` 안에서만).
  `suppressHydrationWarning` 금지.
- 교육용 단순화 허용: 비밀번호 평문 저장과 비교, 서버 시각 기준, 동시 수정은 마지막 저장 우선. 그 밖의 URS 요구는 단순화하지 않는다.

## 9. 수정 요청 모드 (개발 서버 QA 루프)
트리거 예: "수정 요청: …", "다음 오류를 고쳐줘: …", "…화면을 …로 바꿔줘". 한 번에 한 요청씩 처리한다.
1. `PLAN.md`, `IMPLEMENTED.md`, 관련 `SPEC_*.md`, 대상 파일만 읽는다. URS는 요청이 조항을 언급할 때 해당 조항만 확인한다. 전체 재빌드를 하지 않는다.
2. 요청 분류를 첫 줄에 적는다: 결함 수정(URS 요구인데 동작하지 않음), 개선(URS 범위 안의 화면, 문구, 흐름), URS 외 요청(URS에 없는 기능).
   URS 외 요청은 "URS 외 요청입니다. 진행하면 URS 개정 대상으로 기록합니다."라고 알리고, 진행 시 `DECISIONS.md`에 남긴다.
3. 최소 변경. 파일 소유권 분할은 적용하지 않지만 `lib/sheets.ts` 수정 금지, 패키지 추가 금지, 8절 기술 규칙, 2절 표기 규칙, design.md는 그대로 적용한다.
4. 시트 열 이름 변경과 삭제 금지. 열 추가가 필요하면 코드의 헤더 배열에 추가하고, 기존 탭은 사용자에게 1행 맨 끝에 같은 열을 추가하도록 안내한다.
5. 변경 파일의 타입 오류를 확인한다. 개발 서버는 핫리로드되므로 재시작이 필요한 변경(`.env.local`, `next.config.ts`)만 알린다. `npm run build`는 커밋 전에 1회.
6. `CHANGELOG.md`에 한 행 추가(일시 KST, 분류, 요청 요약, 변경 파일, 관련 조항 ID). 구현 상태가 바뀌면 `IMPLEMENTED.md`를 갱신한다.
7. 답변(한국어 보고체): 분류 한 줄, 변경 내용 3~5줄, 확인 방법(어느 화면에서 어떤 조작을 하면 무엇이 보이는지), 미해결 사항.

## 10. 커밋 준비 모드 (명시 요청 시만. 푸시와 배포는 사람이 한다)
트리거 예: "커밋해", "커밋 준비해", "배포 준비해". AI는 `git push`를 실행하지 않는다(이 규칙으로 금지. 사람이 터미널에서 실행한다).
1. `npm run check:commit`으로 커밋 대상에 `.env.local`, 서비스 계정 JSON 키, 개인키 본문, 빌드 산출물이 없는지 확인한다(문제가 있으면 중단하고 파일명을 알린다).
   `npm run check:sheets` OK, `npm run build` 1회 통과. `IMPLEMENTED.md`, `CHANGELOG.md` 최신 확인. 산출물(IMPLEMENTED.md, PLAN.md, DECISIONS.md, CHANGELOG.md, SPEC_*.md, docs/urs)은 함께 커밋한다.
2. `git add -A` 후 한국어 메시지 "무엇을 왜 (URS-F-nnn)". 예: `잔량 초과 출고 차단 로직 추가 (URS-F-027, 위험평가 조치)`. "fix", "update" 같은 메시지 금지.
3. 배포 준비 요청이면 `git tag v1.0`(IOQ 대상 버전)을 만들고 README에 버전을 적는다.
4. 사람이 실행할 명령을 그대로 안내한다: `git push origin main --tags`(원격이 없으면 `git remote add origin <URL>` 먼저). Vercel이 GitHub에 연결되어 있으면 푸시가 곧 배포다.
   배포 후 배포 URL의 `/api/seed`와 로그인 확인, Vercel 환경 변수 3종 확인을 안내한다.

## 11. 산출물 형식 (한국어 보고체)

**IMPLEMENTED.md**: URS 7.1의 모든 조항이 한 행씩 있어야 한다(누락 금지). 7.2 ~ 7.8 중 구현에 반영한 조항도 넣는다.

| URS 조항 ID | 요구 요약 | 상태 | 화면 또는 API | 파일 | 비고 |
|---|---|---|---|---|---|

상태는 `구현`, `부분`, `미구현` 셋 중 하나다. 표 아래에 "미구현 및 부분 구현 항목" 절을 두고 12절 4항 형식의 사유를 조항마다 쓴다.

**DECISIONS.md**: 기본값으로 정한 것(번호, 결정, 근거 조항 또는 "URS 근거 없음"). **PLAN.md**: 시트 탭 스키마, 역할과 계정표, 메뉴, 파일 소유권.
**CHANGELOG.md**: `| 일시 (KST) | 분류 | 요청 요약 | 변경 파일 | 관련 조항 ID |`. **README.md**: 실행 방법, 계정표, 시트 설정, 배포 절차, 시트 탭 구성.

## 12. 완료 보고 형식 (한국어 보고체, 채팅 답변)

1. 실행 방법: `npm run dev`, `/api/seed` 호출, 로그인 화면에서 계정 선택 후 비밀번호 1234.
2. 계정표: URS 역할별 계정 ID, 이름, 역할 코드, 초기 비밀번호(1234), 배포 전 변경 안내.
3. 구현 요약: URS 7.1 조항 수, 구현 수, 부분 수, 미구현 수. 시트 탭 구성표.
4. **미구현 및 부분 구현 항목**: 조항마다 개발자가 사유를 설명하듯 한 문단으로 쓴다. 어떤 요구인지, 왜 구현하지 못했는지(다른 조항과의 불일치, 요구의 모호함,
   시간 예산 초과, 기술 제약), 현재 동작이 어떤지, 보완 방법(URS 개정 제안 또는 수정 요청으로 가능 여부)을 쓴다.
   예: "URS-F-043은 제2자 확인의 기록 항목을 요구합니다. URS-F-042의 승인 기록과 같은 행위를 가리키는데 기록 항목이 달라 두 조항을 함께 충족하는 형태를 정하지 못하였습니다.
   현재는 URS-F-042 기준으로 승인자와 승인 일시만 기록하고 있으며, URS-F-043의 확인 결과 항목을 추가하려면 URS의 두 조항을 하나로 합치는 개정이 필요합니다."
   구현한 항목은 나열하지 않는다(IMPLEMENTED.md로 대신한다).
5. 산출물 위치: `IMPLEMENTED.md`, `DECISIONS.md`, `PLAN.md`, `CHANGELOG.md`, `README.md`.
6. 다음 단계: 개발 서버 QA 후 수정 요청 모드. 구현이 완료되면 FDS 작성 모드(13절).

## 13. FDS 작성 모드 (QA와 수정이 끝나 구현이 완료된 뒤, Word 출력)
트리거 예: "FDS를 작성해줘", "FDS 초안을 만들어줘". 상세 규칙은 `docs/FDS_GUIDE.md`, 양식은 `docs/FDS_TEMPLATE.md`다. 예산 15분.
1. 시스템의 **현재 상태(as-built)** 를 쓴다. 근거는 `IMPLEMENTED.md`, `PLAN.md`, `DECISIONS.md`, `CHANGELOG.md`와 코드(types.ts, app/api, app 화면)다. 코드와 문서가 다르면 코드가 맞다. URS는 조항 ID와 요구 문장 확인에만 쓴다.
2. 목차는 `docs/FDS_TEMPLATE.md`의 1 ~ 6장, 1.1 ~ 4.3절로 고정한다(3.2절 이름만 시스템 고유 기능 영역으로). 양식 문구, 규제 해설, 자리표시, 시험 절차, URS 원문 복사를 쓰지 않는다.
   FS 행 하나는 구현된 기능 하나를 검증 가능한 문장으로 쓴다. 본문 문체는 규격서 평서형("~한다", "~된다")이다.
3. FS 행과 DS 행마다 관련 URS 조항 ID를 적는다. 구현된 7.1 조항은 빠짐없이 어느 행에 연결하고, URS에 없는 기능은 "URS 근거 없음 (DECISIONS.md n번)"으로,
   미구현 조항은 1.2 범위의 제외 목록에 사유와 함께 적는다.
4. `docs/FDS.md` 작성 → `npm run fds`(검증 + Word 문서 생성) → ERROR 0건이 될 때까지 수정 반복 → `CHANGELOG.md`에 한 행 → 보고(`docs/FDS_GUIDE.md` 5절: 파일 경로, FS/DS 건수, URS 대조 결과, 사람이 할 일).
