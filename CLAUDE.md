# 이 폴더는 Codex 전용 하네스다

마스터 프롬프트는 `AGENTS.md`다. 이 파일을 읽은 도구는 `AGENTS.md`의 절차와 규칙을 그대로 따른다.
Claude Code와 Antigravity CLI 조는 `05 Harness` 폴더(서브에이전트 정의가 `.claude/agents/`에 있는 판)를 쓴다.
서브에이전트 정의는 이 폴더의 `agents/` 에 있으며, 서브에이전트를 띄울 때 해당 파일 전문을 프롬프트에 넣는다.
