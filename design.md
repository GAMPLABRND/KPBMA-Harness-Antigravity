# design.md: KPBMA 교육 시스템 디자인 가이드

이 하네스로 만드는 모든 화면은 이 문서를 따른다. 토큰은 `app/globals.css`의 `@theme`에 같은 이름으로 등록되어 있고,
공용 부품은 `components/ui.tsx`에 있다. 빌더는 새 색이나 새 모양을 만들지 않고 여기 정의된 것만 조합한다.
출처: 한국제약바이오협회 CSV 실습 교육 포털(갬프연구소)의 구현 디자인.

## 1. 디자인 방향

- 인상: 협회 주관 교육의 신뢰감 + 현대적인 SaaS형 업무 화면. 차분하고, 전문적이고, 명확하게.
- 정보 밀도는 중간에서 높음. 카드와 여백으로 구조를 만들고, 장식 대신 배지, 표, 안내 상자로 강조한다.
- 피할 것: 화려한 스타트업풍 그래픽, 그라데이션 남용(로그인 배경만 예외), 캐주얼한 일러스트,
  "AI가 모든 것을 자동화한다"는 과장, 실제 GMP 운영 시스템으로 오인될 표현.
- 비율: 흰색과 연회색 70%, 네이비와 검정 글자 15%, 주 파랑 10%, 시안과 상태색 5%.
- 색만으로 상태를 구분하지 않는다. 배지에는 항상 상태 글자를 함께 쓴다.

## 2. CI(로고)와 표기

| 파일 | 용도 | 크기 |
|---|---|---|
| `public/kpbma-ci.png` (444x56, 파란 원본) | 로그인 카드 상단, 인쇄물 머리글, 흰 바탕 | 로그인 카드 폭 최대 420px, 인쇄물 높이 28px |
| `public/kpbma-ci-white.png` (흰색 버전) | 네이비 상단바 | 높이 25px |
| `app/icon.png` (32x32) | 브라우저 탭 아이콘 | 자동 적용 |

- 상단바 왼쪽은 항상 CI + 시스템 제목(`lib/brand.ts`의 `APP_NAME`). 시스템 제목은 "CSV실습과정 [조번호]조 [시스템명]" 형식으로 고정하며(예: "CSV실습과정 1조 전자로그북"),
  브라우저 탭, 로그인 화면 제목, 상단바, 대시보드 제목에 같은 값을 쓴다. `TEAM_NO`와 `SYSTEM_NAME`만 URS대로 바꾼다.
- CI 옆에 다른 기관 로고를 놓지 않는다. CI를 늘리거나 색을 바꾸지 않는다(흰색 버전만 예외).
- 교육용 표기: 화면 하단에 `FOOTER_NOTICE`("본 시스템은 CSV 실습 교육을 위한 교육용 목업 시스템입니다.")를 항상 둔다.
- 용어: 데이터 완전성, 감사추적, 전자서명, 기준정보. 화면 문구는 한국어 평서형, 방점(·)과 대시(—)를 쓰지 않는다.

## 3. 글꼴

- 본문 글꼴: Pretendard (CDN `@import`, 없으면 시스템 글꼴로 대체). 기본 15px, 줄 간격 1.6, 안티앨리어싱.
- 제목(h1~h4)은 네이비(`primary-dark`) 굵게.

| 용도 | 크기 | 굵기 |
|---|---|---|
| 화면 제목 (PageTitle) | 24px | 700 |
| 카드 소제목 | 15px | 700 |
| 모달 제목 | 17px | 700 |
| 본문, 폼 입력 | 14 ~ 15px | 400 |
| 표 | 13.5px (머리글 700) | |
| 라벨, 메타 | 13.5px | 700 (라벨), 400 (메타) |
| 설명, 캡션 | 12.5 ~ 13px | 400 ~ 500 |
| 배지 | 11px | 700, 대문자 |
| KPI 값 | 26px | 800 |

## 4. 색 토큰 (globals.css @theme, Tailwind 클래스로 사용)

| 토큰 | 값 | 클래스 예 | 쓰임 |
|---|---:|---|---|
| `primary` | #0072CE | `bg-primary`, `text-primary`, `border-primary` | 주 버튼, 링크, 활성 메뉴, 강조 |
| `primary-dark` | #163A5F | `bg-primary-dark`, `text-primary-dark` | 상단바, 제목, 모달 덮개 |
| `primary-soft` | #EAF5FF | `bg-primary-soft` | 안내 상자, 선택 항목, 표 행 hover |
| `cyan` | #00A6B2 | `text-cyan` | 보조 포인트, 아이콘 |
| `muted` | #F5F7FA | `bg-muted` | 화면 바탕, 표 머리글, KPI 타일 |
| `line` | #DDE3EA | `border-line`, `divide-line` | 테두리, 구분선 |
| `ink` | #1F2933 | `text-ink` | 본문 글자 |
| `ink-muted` | #667085 | `text-ink-muted` | 설명, 메타 |
| `success` | #12B76A | `text-success`, `bg-success` | 승인, 완료, 정상, 사용가능 |
| `warning` | #F79009 | `text-warning`, `bg-warning` | 대기, 주의, 만료 임박 |
| `danger` | #F04438 | `text-danger`, `bg-danger` | 반려, 오류, 차단, 이상 |
| `rounded-card` | 16px | `rounded-card` | 카드, 모달, 로그인 카드 |
| `rounded-input` | 8px | `rounded-input` | 입력, 알림 |
| `rounded-pill` | 50px | `rounded-pill` | 버튼, 배지, 역할 표시 |
| `shadow-card` | 옅은 그림자 | `shadow-card` | 카드 |
| `shadow-modal` | 짙은 그림자 | `shadow-modal` | 모달 |

배지 전용 색(상태 글자와 함께 쓴다): 초안과 사용중은 노랑(#FEF08A/#854D0E), 제출과 요청과 완료는 파랑(#BFDBFE/#1E40AF),
승인과 검토완료와 활성과 정상은 초록(#A7F3D0/#065F46), 출력은 보라(#DDD6FE/#5B21B6), 반려와 무효와 이상은 빨강(#FCA5A5/#991B1B),
사용중지와 수정요청은 주황(#FED7AA/#9A3412), 비활성과 폐기는 회색. `StatusBadge`의 BADGE_COLORS 표에 상태 코드를 등록해 쓴다.

## 5. 화면 골격

### 5.1 상단바 (`app/layout.tsx`)
- 높이 56px, 바탕 `primary-dark`, 글자 흰색, 아래 얇은 그림자.
- 왼쪽: 흰색 CI(높이 25px) + 시스템명(16px 굵게). 오른쪽: 사용자 ID(굵게) + 역할 배지(`bg-primary` 알약, 11px, "역할명 (코드)") + 로그아웃(테두리 알약 버튼).
- 인쇄 시 숨김(`no-print`).

### 5.2 사이드 메뉴 (`components/SideNav.tsx`)
- 폭 220px, 흰 바탕, 오른쪽 `line` 테두리. 메뉴 항목은 URS §6.2 화면 목록과 §6.3 권한 매트릭스대로 역할별로 필터한다.
- 그룹 소제목: 네이비 14px 굵게, 아래 2.5px 파란 선. 항목: 14px, 좌측 4px 투명 선, hover 시 `bg-muted` + 파란 글자,
  활성 시 `bg-primary-soft` + 좌측 파란 선 + 파란 굵은 글자.

### 5.3 본문
- 바탕 `muted`, 안쪽 여백 40px 32px, 내용 최대 폭 1152px 가운데 정렬. 화면 맨 위는 `PageTitle`(제목 + 한 줄 설명 + 우측 동작 버튼).
- 하단 풋터: 교육용 목업 고지 + 과정명, 12px 회색, 위쪽 `line` 구분선. 인쇄 시 숨김.

### 5.4 화면 구성 순서
대시보드 → 기준정보 → 업무 등록 → 업무 기록 목록과 상세 → 검토와 승인 → 출력 → 알람 → 감사추적 → 비밀번호 변경 → 관리자 설정.
각 화면은 "제목 영역 → (안내 상자) → 카드(폼 또는 표)" 순으로 쌓는다. 한 화면에 카드 1~3개.

## 6. 부품 (components/ui.tsx)

| 부품 | 모양 | 쓰는 곳 |
|---|---|---|
| `PageTitle` | 네이비 24px 제목 + 회색 설명 + 우측 버튼 | 모든 화면 맨 위 |
| `Card` | 흰 바탕, `rounded-card`, `line` 테두리, `shadow-card`, 안쪽 24px, 소제목 15px | 폼, 표, 상세 정보 묶음 |
| `Kpi` | `muted` 바탕 타일, 라벨 12.5px 회색 + 값 26px 네이비(`tone`으로 색 변경) | 대시보드 집계(3~4열 `grid`) |
| `Button` | 알약형, primary(파랑), secondary(흰색 테두리), danger(빨강), `size="sm"` | 저장, 취소, 반려, 목록 새로고침 |
| `Field` + `TextInput`/`Select`/`Textarea` | 라벨 13.5px 굵게, 필수 별표 빨강, 입력 `rounded-input`, 포커스 파란 테두리 + 연파랑 링, 오류 빨간 문구 | 모든 폼 |
| `Table` | 머리글 `muted` 바탕 네이비 굵게, 행 구분선, hover `primary-soft`, 빈 상태 문구. `columns`(열 폭과 머리글 정렬), `density`("normal", "compact" 셀 여백). 항상 `table-fixed`와 `overflow-x-auto` 상자를 갖는다(열 폭이 어긋나도 카드 폭 불변) | 목록 |
| `StatusBadge` | 알약 11px 대문자, 상태별 색 + 한글 라벨 | 기록 상태, 장비 상태, 계정 상태 |
| `NoticeBox` | 연파랑 상자, 제목 + 본문 | 규정 고지, 작성 안내, 차단 규칙 설명 |
| `Modal` | 네이비 반투명 덮개 + 흐림, 흰 카드 `rounded-card` 28px 여백, 제목 17px, `size`로 폭 선택(md 512px, lg 768px, xl 1024px) | 전자서명, 사유 입력, 확인, 항목이 많은 상세 |
| `Banner` | info(연파랑), warn(노랑), error(빨강), success(초록) 한 줄 알림 | 차단 안내(URS 원문), 저장 결과 |
| `Td` / `Th` | 셀 정렬 부품(`align`, `nowrap`, `num`, `code`, `clamp`) | 모든 목록 표의 셀 |
| `DescList` | 라벨 128px + 값의 정의형 목록. 컨테이너 폭 기준으로 1열과 2열을 자동 전환 | 상세 화면, 감사추적 상세 모달 |
| `PrintHeader` | 파란 CI 28px + 문서 제목 + 문서번호, 출력자, 출력 일시 | 모든 인쇄용 화면 머리글 |
| `DocTable` | 검정 테두리 문서 표(`doc-table`) | 인쇄용 화면의 표 |
| `PrintButton` | 인쇄 버튼. `auditEndpoint`를 주면 출력 감사추적을 먼저 기록하고 인쇄창을 연다 | 모든 인쇄 화면의 인쇄 시작 |
| `AuditPrintTable` | 감사추적 보고서 인쇄 표(요약 5열 + 기록별 대상, 변경 전후, 사유 블록) | 감사추적 보고서 인쇄 |

### 6.1 폼 규칙
- 한 줄에 라벨 하나, 입력 하나. 필수 항목은 별표. 시스템이 자동 부여하는 값(작성자, 일시)은 읽기 전용 회색 입력으로 보여 준다.
- 선택 항목(장비, 사용 유형, 상태)은 `Select`. 사유 입력은 `Textarea` 2~3줄.
- 저장 전 확인이 필요한 중요 입력은 `Modal`로 입력 내용을 한 번 더 보여 준다(URS 정확성 확인 요건).
- 버튼 순서: 오른쪽 정렬, 보조(취소) → 주 동작(저장). 위험 동작(무효 처리, 반려)은 danger.

### 6.2 목록 규칙
- **표가 든 카드는 좌우로 나란히 두지 않는다.** 표 카드는 화면 전체 폭 1열로 위아래로 쌓는다(대시보드 포함).
  좌우 grid 배치는 KPI 타일과 표가 없는 짧은 요약 카드만 하며, grid 셀에는 `min-w-0`을 준다.
- 표 위에 조회 조건(Select, 날짜 입력)과 "조회", "초기화", "새로고침" 버튼을 한 줄로 둔다. 자동 새로고침(폴링)은 없다.
- 상태 열은 `StatusBadge`. 일시 열은 `YYYY-MM-DD HH:mm (KST)`.
- 행 끝에 "상세" 링크 또는 작은 버튼. 행 전체 클릭 이동은 쓰지 않는다.
- 목록 표의 열은 7개 이내로 한다. 초과하면 (1) 의미가 같은 값을 한 열로 병합한다(변경 전과 변경 후를 "변경 요약" 한 열로),
  (2) 부가 정보는 상세 모달로 옮긴다. 표에는 항상 overflow 상자가 있어 실수해도 카드 폭은 변하지 않지만, 스크롤바가 보이는 표는 열 폭이나 열 수를 고친다.
- 열 폭은 `columns`의 `width`로 지정하여 `table-fixed`로 고정한다. 긴 값은 가로로 늘리지 않고 줄바꿈으로 처리한다.
- 셀은 상단 정렬로 통일하고 행 높이는 본문 2줄을 상한으로 한다. 전문은 상세에서 보여 준다.
- 열 폭은 실제 표기 길이로 산정한다. 줄바꿈을 금지한 열(`nowrap`, `code`, `num`)은 폭을 넘으면 말줄임으로 잘리므로, 초를 포함한 일시는 200px 이상, 코드와 문서번호는 표기 길이에 맞춘 폭을 준다.
- 버튼 글자는 줄바꿈되지 않는다. "상세" 같은 동작 열은 88px 이상을 준다. 폭이 좁으면 버튼이 세로로 눌린 모양이 된다.
- 셀 여백은 `Table`이 소유한다. `Td`에 `px-*`, `py-*` 같은 여백 클래스를 붙이지 않는다. 줄 수 상한은 `Td`의 `clamp`로만 주며, 부품이 셀 안쪽 요소에 적용한다.

열별 정렬 규격:

| 열 종류 | 가로 정렬 | 줄바꿈 | 부품 사용 |
|---|---|---|---|
| 일시 | 왼쪽 | 금지 | `Td nowrap code`, 표기 `YYYY-MM-DD HH:mm (KST)` |
| 코드, ID, 문서번호 | 왼쪽 | 금지 | `Td code` |
| 이름, 행위자, 대상 | 왼쪽 | 허용(2줄) | `Td clamp={2}` |
| 수량, 금액, 순번 | 오른쪽 | 금지 | `Td num` |
| 상태 배지 | 가운데 | 금지 | `Td align="center"` + `StatusBadge` |
| 사유, 변경 요약, 비고 | 왼쪽 | 허용(2줄) | `Td clamp={2}` |
| 동작(상세 버튼) | 가운데 | 금지 | `Td align="center"` |

### 6.3 전자서명 모달
- 제목 "검토 완료 전자서명" 등 행위명. 본문에 `NoticeBox`로 전자서명 고지(서명의 효력, 본인 비밀번호 확인 필요)를 한 번 보여 주고,
  서명자(읽기 전용), 비밀번호 입력, 서명 의미(검토 완료 등)를 둔다. 완료 후 `Banner success`로 "전자서명이 되었습니다."를 표시한다.

### 6.4 차단 안내
- 서버가 거부한 요청의 안내 문구는 URS 원문 그대로 `Banner error`로 보여 준다(예: "종료일시는 시작일시 이후여야 합니다.").

### 6.5 감사추적과 다열 표 표시 규칙
- AUDIT 탭은 11열이지만 화면 목록은 데이터 7열과 동작 열 1개로 줄인다: 일시(200px, 초 포함 표기 기준), 분류(80px), 행위자(130px, 이름과 역할), 행위(130px), 대상(150px), 변경 요약(폭 미지정, 잔여 폭), 사유(120px), 동작(88px).
- `before_value`와 `after_value`는 "변경 전 → 변경 후" 형식의 변경 요약 한 열로 합치고 2줄까지 표시한다(화살표는 상태 흐름 표기이므로 허용).
- 행 끝 "상세" 버튼의 `Modal`(`size="xl"`) 안에서 `DescList`로 11개 필드 전부를 보여 준다. 행 전체 클릭 이동은 쓰지 않는다(6.2 유지).
- 일시 열은 초까지 표기하므로 200px 미만으로 줄이지 않는다. 폭이 부족하면 옆 열과 글자가 겹치는 대신 말줄임으로 잘린다.
- 목록에 가로 스크롤바가 보이면 완성 상태가 아니다. 열 폭이나 열 수를 고친다(overflow 상자는 안전망이다).

### 6.6 상세 정보 표시 규칙
- 상세 화면과 상세 모달의 라벨과 값은 `DescList`로 만든다. 라벨 폭 128px, 값 좌측 정렬, 긴 값은 줄바꿈.
- 1열과 2열 전환은 화면 폭이 아니라 컨테이너 폭을 기준으로 부품이 자동 처리한다. 좁은 컨테이너에서는 1열이 되고 라벨이 값 위로 올라간다.
- 모달 안에서 쓸 때는 `Modal`의 `size`를 항목 수에 맞게 넓힌다. 항목이 8개를 넘으면 `size="xl"`을 쓴다.
- 값이 긴 항목(사유, 변경 내용)은 `full: true`로 두 열을 다 쓴다.
- 라벨과 값을 표(`Table`)로 만들지 않는다.

## 7. 로그인 화면 (`app/login`)

- 전체 화면 네이비 방사 그라데이션 배경 위에 흐르는 블롭 4개(`.aurora-bg`, 그라데이션이 허용되는 유일한 곳).
- 가운데 폭 560px 유리 카드(`bg-white/95`, 흐림, `rounded-card`, 48px 여백, 짙은 그림자).
- 카드 안 순서: 파란 CI(폭 최대 420px) → 시스템명(18.5px 네이비) → 부제(12.5px 회색) → 환경 변수 경고(있을 때만)
  → 계정 선택 드롭다운(USERS 탭의 활성 계정 목록, "이름 (아이디, 역할)") → 아이디(드롭다운에서 고른 계정이 채워짐, 직접 입력 가능) → 비밀번호(마스킹. 초기 비밀번호 상태인 계정을 고르면 자동 입력) → 로그인 버튼(전체 폭)
  → 계정 안내 상자(`/api/seed`, 초기 비밀번호 안내) → 교육용 목업 고지.
- 계정 목록이 비어 있으면(시드 전) 드롭다운을 숨기고 아이디 직접 입력만 남긴다. 이 구조(드롭다운 → 아이디 → 비밀번호)는 바꾸지 않는다.

## 8. 출력물(인쇄 화면)

- 인쇄 대상은 `print-area` 클래스 영역이며 흰 바탕, 검정 글자로 만든다. 머리글은 `PrintHeader`(파란 CI 높이 28px, 문서 제목, 문서번호, 출력자, 출력 일시), 표는 `DocTable`(`doc-table`, 1.5px 검정 테두리)을 쓴다.
- 용지는 A4, 여백 10mm이다. 열이 7개를 넘는 보고서는 `print-area` 최상위 요소에 `print-landscape`를 붙여 가로 방향으로 출력한다.
  Chrome과 Edge에서 동작하고 Firefox는 세로로 출력되므로, 세로에서도 읽히도록 인쇄 열 수를 줄인다.
- `DocTable`에는 본문 `<tr>` 행만 넘긴다. `<colgroup>`, `<thead>`, `<tbody>`는 부품이 만든다.
- 감사추적 보고서의 인쇄 표는 `AuditPrintTable`로 만든다(요약 5열: 일시, 분류, 행위자, 역할, 행위 유형 + 기록별 대상, 변경 전후, 사유 상세 블록). 화면에서 2줄로 줄인 값은 인쇄물에서 전문을 출력한다.
- 인쇄는 `PrintButton`으로 시작한다: 출력자와 출력 일시의 감사추적을 기록한 뒤 브라우저 인쇄창을 연다(URS의 출력 기록 요구 대응).
- 표 머리글은 매 페이지 반복되며(`thead`의 `table-header-group`), 행은 페이지 중간에서 분할되지 않는다.
  카드 단위로 페이지를 나눌 때는 `print-break-before`, 묶음 유지는 `print-avoid-break`를 쓴다.
- 인쇄 표 글자는 10.5px, 셀 여백은 3px 5px로 축소된다. 상단바, 사이드 메뉴, 버튼, 풋터는 `no-print`로 숨긴다.
- 페이지 번호는 CSS로 넣지 않는다. 브라우저 인쇄 옵션의 머리글과 바닥글을 쓰도록 화면에 안내 문구를 둔다.
- 미승인 상태면 "DRAFT" 워터마크를 겹친다. 서명란은 2열 격자(검토자 서명 정보, 승인 정보) 상자로 표시한다.
- 인쇄 미리보기 확인 항목: 가로 스크롤로 잘린 열이 없는지, 각 페이지에 표 머리글이 있는지, 행이 중간에서 잘리지 않는지, 상태 배지 색이 남는지.

## 9. 문구

- 화면 문구는 한국어 평서형. 버튼은 동사형 명사("저장", "사용 시작 등록", "검토 완료"). 표 머리글과 라벨은 명사구.
- 오류와 차단 문구는 원인과 할 일을 한 문장에(예: "교정 유효기간이 지난 장비는 사용을 시작할 수 없습니다.").
- 방점(·), 대시(—), 이모지, 장식 기호를 쓰지 않는다. 화살표(→)는 상태 흐름 표기에만 쓴다.
- 교육용 표기를 숨기지 않는다(풋터 고지 유지). 실제 GMP 운영 시스템처럼 보이게 하는 문구("Part 11 검증 완료" 등)를 쓰지 않는다.

## 10. 반응형과 접근성

- 기준 폭은 데스크톱(1280 이상). 본문 표는 열 폭 배분(`columns`의 `width`)과 줄바꿈으로 본문 폭(1152px) 안에 넣는다.
  가로 스크롤(`Table`의 `scroll`)은 마지막 수단이며, 열이 7개를 넘으면 열을 병합하거나 상세로 옮긴다. 본문 영역(`main`)에는 가로 스크롤을 두지 않는다.
- 입력과 버튼은 키보드로 조작 가능해야 하며 포커스 링을 없애지 않는다. 상태는 색과 글자를 함께 쓴다.
- 본문 글자는 13px 이상, 대비는 회색 글자도 `ink-muted`(#667085) 이상을 쓴다.
